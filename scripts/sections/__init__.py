"""Section render modules for generate_main_strategy_v2_report.py.

Each module exports build_*_section and render_*_section pairs that take
the report payload dict and return data / list[str]. Extracted from the
monolith per REFACTOR_PLAN.md phase B.
"""
